## What changed

<!-- One or two sentences. Link an issue if there is one. -->

## How to verify

<!-- Steps a reviewer can follow, or the command they should run. -->

## Checklist

- [ ] `npm run lint`, `npm test` and `npm run build` pass
- [ ] User-visible strings go through i18n (both `en` and `pl`)
- [ ] Nothing new leaves the device, or the privacy dialog and README say so
